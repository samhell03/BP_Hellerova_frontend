{/*import TripCard from "./TripCard";

function TripsList() {
  const trips = [
    { id: 1, country: "Itálie", startDate: "2026-03-01" },
    { id: 2, country: "Francie", startDate: "2026-04-15" },
  ];

  return (
    <div className="bg-white p-6 rounded-2xl shadow">
      <h2 className="text-xl font-bold mb-4">Moje výlety</h2>

      <div className="space-y-3">
        {trips.map(trip => (
          <TripCard key={trip.id} trip={trip} />
        ))}z
      </div>
    </div>
  );
}

export default TripsList; */}
